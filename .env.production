# Replace with the deployed backend API URL once it exists, then also set
# VITE_API_URL in the Vercel dashboard (the dashboard value is what production uses).
VITE_API_URL=https://your-deployed-api-url.example.com
